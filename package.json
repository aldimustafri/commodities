{
  "name": "commodity_resources",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf node_modules/ && rm -rf package-lock.json && rm -rf .next/",
    "dev": "next dev -p 9350",
    "dev:clean": "rm -rf node_modules/ && rm -rf package-lock.json && rm -rf .next/ && npm install && npm run dev",
    "build": "next build",
    "build:clean": "rm -rf node_modules/ && rm -rf package-lock.json && rm -rf .next/ && npm install && npm run build",
    "start": "next start -p ${PORT:-9350}",
    "lint": "next lint"
  },
  "dependencies": {
    "@ant-design/icons": "^4.6.2",
    "@next/bundle-analyzer": "^9.1.4",
    "antd": "^4.16.6",
    "cross-env": "^7.0.2",
    "next": "11.1.2",
    "postcss-preset-env": "^6.7.0",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "lodash": "^4.17.21",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "eslint": "8.0.1",
    "eslint-config-next": "11.1.2"
  },
  "browser": {
    "fs": false,
    "path": false
  }
}
